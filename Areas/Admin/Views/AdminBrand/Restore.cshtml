@model HuynhMinhTri_2122110256.Models.Brand
@{
    ViewBag.Title = "Restore";
}

<div class="container mt-3">
    <h2 class="text-success text-center">Khôi phục thương hiệu</h2>

    <h3 class="text-center text-warning">Bạn chắc chắn muốn khôi phục thương hiệu này?</h3>

    <div class="card mt-4 p-4">
        <h4 class="text-center">Chi tiết thương hiệu</h4>
        <hr />
        <div class="row">
            <div class="col-md-6">
                <div class="form-group row">
                    <label class="col-sm-4 font-weight-bold">Tên thương hiệu:</label>
                    <div class="col-sm-8">
                        <p>@Html.DisplayFor(model => model.brandName)</p>
                    </div>
                </div>
                <div class="form-group row">
                    <label class="col-sm-4 font-weight-bold">Thuộc sản phẩm:</label>
                    <div class="col-sm-8">
                        @if (Model.status == 1)
                        {
                            <p> Hoạt động</p>

                        }
                        else
                        {
                            <p> Hủy hợp tác</p>
                        }

                    </div>
                </div>
                <div class="form-group row">
                    <label class="col-sm-4 font-weight-bold">Mô tả:</label>
                    <div class="col-sm-8">
                        <p>@Html.DisplayFor(model => model.description)</p>
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="form-group row">
                    <label class="col-sm-2 font-weight-bold">Hình ảnh:</label>
                    <div class="col-sm-10">
                        <img src="~/Content/images/brand/@Model.image" alt="Category Image" class="img-fluid" style="max-width: 200px;" />
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="text-center my-4">
        @using (Html.BeginForm("Restore", "AdminBrand", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            @Html.AntiForgeryToken()

            <button type="submit" class="btn btn-success btn-lg">Khôi phục</button>
        }
    </div>
</div>


