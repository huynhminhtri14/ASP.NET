@model HuynhMinhTri_2122110256.Models.User

@{
    ViewBag.Title = "Delete User";
}

<div class="container mt-3">
    <h2 class="text-danger text-center">Xóa người dùng</h2>

    <h3 class="text-center text-warning">Bạn chắc chắn muốn xóa người dùng này?</h3>

    <div class="card mt-4 p-4">
        <h4 class="text-center">Chi tiết người dùng</h4>
        <hr />
        <div class="row">
            <div class="col-md-6">
                <div class="form-group row">
                    <label class="col-sm-4 font-weight-bold">Tên người dùng:</label>
                    <div class="col-sm-8">
                        <p>@Model.first_name @Model.last_name</p>
                    </div>
                </div>
                <div class="form-group row">
                    <label class="col-sm-4 font-weight-bold">Địa chỉ:</label>
                    <div class="col-sm-8">
                        <p>@Model.address</p>
                    </div>
                </div>
                <div class="form-group row">
                    <label class="col-sm-4 font-weight-bold">Số điện thoại:</label>
                    <div class="col-sm-8">
                        <p>@Model.phone</p>
                    </div>
                </div>
                <div class="form-group row">
                    <label class="col-sm-4 font-weight-bold">Email:</label>
                    <div class="col-sm-8">
                        <p>@Model.email</p>
                    </div>
                </div>
                <div class="form-group row">
                    <label class="col-sm-4 font-weight-bold">Quyền hạn:</label>
                    <div class="col-sm-8">
                        @if (Model.role == 1)
                        {
                            <p> Admin</p>

                        }
                        else
                        {
                            <p> Khách hàng</p>
                        }

                    </div>
                </div>
            </div>
    </div>

    <div class="text-center my-4">
        @using (Html.BeginForm("Delete", "AdminUser", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            @Html.AntiForgeryToken()

            <button type="submit" class="btn btn-danger btn-lg">Xóa</button>
            <a href="@Url.Action("Index")" class="btn btn-secondary btn-lg ml-3">Quay về danh sách</a>
        }
    </div>
</div>
